# Ambiente del proyecto. Fijar versiones EXACTAS: es lo que permite que otra
# persona obtenga el mismo número, no sólo que el código corra.
# Generar con:  pip freeze > requirements.txt

numpy==1.26.4
pandas==2.2.3
statsmodels==0.14.6
matplotlib==3.9.4
